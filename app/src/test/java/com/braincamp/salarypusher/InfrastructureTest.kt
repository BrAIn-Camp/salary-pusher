package com.braincamp.salarypusher

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Trivial test to confirm the unit test infrastructure is wired up correctly.
 * If this test fails, the test runner configuration is broken.
 */
class InfrastructureTest {

    @Test
    fun testInfrastructure_trivialAssertionPasses() {
        assertEquals("Test infrastructure is working", 2, 1 + 1)
    }
}
