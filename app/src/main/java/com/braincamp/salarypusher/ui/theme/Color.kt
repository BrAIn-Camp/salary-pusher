package com.braincamp.salarypusher.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background ────────────────────────────────────────────────────────────────
val ArcadeDark        = Color(0xFF0D0B1E)  // Deep arcade backdrop
val ArcadeSurface     = Color(0xFF1A1535)  // Slightly lighter surface
val ArcadeElevated    = Color(0xFF252047)  // Cards, dialogs

// ── Gold / Coin ───────────────────────────────────────────────────────────────
val CoinGold          = Color(0xFFFFD700)  // Primary gold — buttons, accents
val CoinGoldDark      = Color(0xFFB8860B)  // Gold shadows, secondary text
val CoinGoldLight     = Color(0xFFFFF176)  // Highlights on gold

// ── Silver ────────────────────────────────────────────────────────────────────
val CoinSilver        = Color(0xFFC0C0C0)  // Dimes, nickels
val CoinSilverDark    = Color(0xFF808080)

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary       = Color(0xFFFFFFFF)
val TextSecondary     = Color(0xFFB0A8D0)  // Muted purple-white
val TextDisabled      = Color(0xFF5A5270)

// ── Status ────────────────────────────────────────────────────────────────────
val EarningsGreen     = Color(0xFF4CAF50)
val ErrorRed          = Color(0xFFCF6679)

// ── Neon accents (for future visual polish) ───────────────────────────────────
val NeonPurple        = Color(0xFF9C27B0)
val NeonBlue          = Color(0xFF2196F3)
